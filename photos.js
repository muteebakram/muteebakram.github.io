window.photoTrips = {
  "01": {
    Name: "Seattle & Olympic National Park",
    Year: "2026",
  },
  "02": {
    Name: "UofU & Salt Lake City",
    Year: "2026",
  },
  "03": {
    Name: "Monterey, Carmel & Big Sur",
    Year: "2026",
  },
  "04": {
    Name: "Mammoth Lakes & Inyo National Forest",
    Year: "2026",
  },
};

window.photos = [
  {
    Id: "mammoth-lake-mary",
    Src: "./assets/trips/04/DSC01168.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01168.jpeg",
    Title: "Lake Mary",
    Alt: "Lake Mary in the Mammoth Lakes area of the Eastern Sierra",
    Date: "Jun 18, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mammoth Lakes, USA",
  },
  {
    Id: "mammoth-lake-road",
    Src: "./assets/trips/04/DSC01189.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01189.jpeg",
    Title: "Road into the Sierra",
    Alt: "Road leading toward dramatic mountain peaks in the Mammoth Lakes area of the Eastern Sierra",
    Date: "Jun 18, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mammoth Lakes, USA",
  },
  {
    Id: "shaded-lakeshore",
    Src: "./assets/trips/04/DSC01210.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01210.jpeg",
    Title: "Shaded Lakeshore",
    Alt: "Sunlit alpine lake water framed by branches and granite rocks",
    Date: "Jun 18, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mammoth Lakes, USA",
  },
  {
    Id: "june-lake-panorama",
    Src: "./assets/trips/04/DSC01225.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01225.jpeg",
    Title: "June Lake Panorama",
    Alt: "Panorama of June Lake and the surrounding mountains in the June Lake area of the Eastern Sierra",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mammoth Lakes, USA",
  },
  {
    Id: "mono-lake-tufa",
    Src: "./assets/trips/04/DSC01237.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01237.jpeg",
    Title: "Mono Lake Tufa",
    Alt: "Tufa towers and blue water at Mono Lake beneath distant mountains",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mono Lake, USA",
  },
  {
    Id: "tioga-lake",
    Src: "./assets/trips/04/DSC01319.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01319.jpeg",
    Title: "Tioga Lake",
    Alt: "Tioga Lake surrounded by pine forest and snow-covered Sierra peaks",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Tioga Pass, USA",
  },
  {
    Id: "tioga-pass-peaks",
    Src: "./assets/trips/04/DSC01335.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01335.jpeg",
    Title: "Tioga Pass Peaks",
    Alt: "Snow-streaked granite peaks rising above the Tioga Pass forest",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Tioga Pass, USA",
  },
  {
    Id: "tuolumne-meadow-stream",
    Src: "./assets/trips/04/DSC01379.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01379.jpeg",
    Title: "Tuolumne Meadow Stream",
    Alt: "Winding stream crossing Tuolumne Meadows below a snow-streaked ridge",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Tioga Pass, USA",
  },
  {
    Id: "tuolumne-meadows",
    Src: "./assets/trips/04/DSC01483.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01483.jpeg",
    Title: "Tuolumne Meadows",
    Alt: "Tuolumne River and footbridge crossing a broad mountain meadow",
    Date: "Jun 19, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Tioga Pass, USA",
  },
  {
    Id: "lakeside-e-bikes",
    Src: "./assets/trips/04/DSC01180.jpeg",
    CachedSrc: "./assets/trips/04/Cached/Cached_DSC01180.jpeg",
    Title: "Lakeside E-Bikes",
    Alt: "Two electric bicycles parked beside a lake trail in the Mammoth Lakes area of the Eastern Sierra",
    Date: "Jun 18, 2026",
    Year: "2026",
    Trip: "04",
    Location: "Mammoth Lakes, USA",
  },
  {
    Id: "highway-one",
    Src: "./assets/trips/03/DSC01078.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC01078.jpeg",
    Title: "Highway 1",
    Alt: "Highway 1 winding between Big Sur hills and the Pacific Ocean",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Big Sur, USA",
  },
  {
    Id: "big-sur-golden-coast",
    Src: "./assets/trips/03/DSC01063.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC01063.jpeg",
    Title: "Golden Big Sur Coast",
    Alt: "Big Sur cliffs and rocky Pacific coastline in warm evening light",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Big Sur, USA",
  },
  {
    Id: "monterey-wildflowers",
    Src: "./assets/trips/03/DSC00900.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC00900.jpeg",
    Title: "Coastal Wildflowers",
    Alt: "Pink and white coastal wildflowers blooming on the Monterey Peninsula",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Monterey Peninsula, USA",
  },
  {
    Id: "monterey-rocky-shore",
    Src: "./assets/trips/03/DSC00906.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC00906.jpeg",
    Title: "Rocky Shore",
    Alt: "Rocky Monterey Peninsula shoreline with a small sandy beach",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Monterey Peninsula, USA",
  },
  // {
  //   Id: "bixby-creek-bridge",
  //   Src: "./assets/trips/03/DSC01144.jpeg",
  //   CachedSrc: "./assets/trips/03/Cached/Cached_DSC01144.jpeg",
  //   Title: "Bixby Creek Bridge",
  //   Alt: "Bixby Creek Bridge spanning coastal cliffs in Big Sur",
  //   Date: "May 25, 2026",
  //   Year: "2026",
  //   Trip: "03",
  //   Location: "Big Sur, USA",
  // },
  {
    Id: "carmel-beach",
    Src: "./assets/trips/03/DSC00935.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC00935.jpeg",
    Title: "Carmel Beach",
    Alt: "Carmel Beach curving beneath green coastal hills",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Carmel-by-the-Sea, USA",
  },
  {
    Id: "big-sur-rocky-cove",
    Src: "./assets/trips/03/DSC01116.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC01116.jpeg",
    Title: "Rocky Cove",
    Alt: "Rocky Big Sur cove surrounded by green coastal bluffs",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Big Sur, USA",
  },
  {
    Id: "big-sur-headland",
    Src: "./assets/trips/03/DSC01118.jpeg",
    CachedSrc: "./assets/trips/03/Cached/Cached_DSC01118.jpeg",
    Title: "Pacific Headland",
    Alt: "Green Big Sur headland meeting waves along the Pacific coast",
    Date: "May 24, 2026",
    Year: "2026",
    Trip: "03",
    Location: "Big Sur, USA",
  },
  {
    Id: "university-foothills",
    Src: "./assets/trips/02/DSC00665.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00665.jpeg",
    Title: "University Foothills",
    Alt: "Block U on the foothills above the University of Utah",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "University of Utah, USA",
  },
  {
    Id: "salt-lake-city-foothills",
    Src: "./assets/trips/02/DSC00757.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00757.jpeg",
    Title: "Salt Lake City Foothills",
    Alt: "Green foothills rising above the University of Utah campus",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "University of Utah, USA",
  },
  {
    Id: "salt-lake-city-skyline",
    Src: "./assets/trips/02/DSC00667.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00667.jpeg",
    Title: "Utah State Capitol Skyline",
    Alt: "Utah State Capitol skyline with the mountains",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "Salt Lake City, USA",
  },
  {
    Id: "capitol-and-wasatch-range",
    Src: "./assets/trips/02/DSC00774.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00774.jpeg",
    Title: "Capitol and Wasatch Range",
    Alt: "Historic Salt Lake City building with the Wasatch Range behind it",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "Salt Lake City, USA",
  },
  {
    Id: "utah-state-capitol",
    Src: "./assets/trips/02/DSC00769.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00769.jpeg",
    Title: "Utah State Capitol",
    Alt: "Columns and dome of the Utah State Capitol under a blue sky",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "Utah State Capitol, USA",
  },
  {
    Id: "capitol-dome",
    Src: "./assets/trips/02/DSC00770.jpeg",
    CachedSrc: "./assets/trips/02/Cached/Cached_DSC00770.jpeg",
    Title: "Capitol Dome",
    Alt: "Close view of the Utah State Capitol dome",
    Date: "May 10, 2026",
    Year: "2026",
    Trip: "02",
    Location: "Utah State Capitol, USA",
  },
  // {
  //   Id: "university-campus-store",
  //   Src: "./assets/trips/02/DSC00719.jpeg",
  //   CachedSrc: "./assets/trips/02/Cached/Cached_DSC00719.jpeg",
  //   Title: "University Campus Store",
  //   Alt: "University of Utah campus store beneath a clear blue sky",
  //   Date: "May 10, 2026",
  //   Year: "2026",
  //   Trip: "02",
  //   Location: "University of Utah, USA",
  // },
  // {
  //   Id: "capitol-rotunda",
  //   Src: "./assets/trips/02/DSC00828.jpeg",
  //   CachedSrc: "./assets/trips/02/Cached/Cached_DSC00828.jpeg",
  //   Title: "Capitol Rotunda",
  //   Alt: "Murals and chandelier inside the Utah State Capitol rotunda",
  //   Date: "May 10, 2026",
  //   Year: "2026",
  //   Trip: "02",
  //   Location: "Utah State Capitol, USA",
  // },
  {
    Id: "seattle-skyline",
    Src: "./assets/trips/01/DSC00186.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00186.JPEG",
    Title: "Seattle Skyline",
    Alt: "Seattle skyline with the Space Needle and Mount Rainier at sunset",
    Date: "Apr 24, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Seattle, USA",
  },
  {
    Id: "hurricane-ridge",
    Src: "./assets/trips/01/DSC00333.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00333.JPEG",
    Title: "Hurricane Ridge",
    Alt: "Person walking toward snow-covered mountains at Hurricane Ridge",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
  {
    Id: "olympic-forest-floor",
    Src: "./assets/trips/01/DSC00396.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00396.JPEG",
    Title: "Olympic Forest",
    Alt: "Moss-covered fallen trees and ferns in an Olympic National Park forest",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
  {
    Id: "forest-waterfall",
    Src: "./assets/trips/01/DSC00414.jpg",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00414.jpg",
    Title: "Forest Waterfall",
    Alt: "Waterfall descending through a dense Olympic National Park forest",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
  {
    Id: "port-angeles-shoreline",
    Src: "./assets/trips/01/DSC00445.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00445.JPEG",
    Title: "Port Angeles Shoreline",
    Alt: "Yellow bus parked along the Port Angeles shoreline",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Port Angeles, USA",
  },
  {
    Id: "port-angeles-beach",
    Src: "./assets/trips/01/DSC00454.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00454.JPEG",
    Title: "Beach at Sunset",
    Alt: "Rocky Port Angeles beach at sunset",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Port Angeles, USA",
  },
  {
    Id: "port-angeles-sunset",
    Src: "./assets/trips/01/DSC00462.jpg",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00462.jpg",
    Title: "Sunset on the Strait",
    Alt: "Golden sunset reflecting on the water near Port Angeles",
    Date: "Apr 25, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Port Angeles, USA",
  },
  {
    Id: "curved-forest",
    Src: "./assets/trips/01/DSC00537.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00537.JPEG",
    Title: "Curved Forest",
    Alt: "Curved tree trunks rising through a green Olympic National Park forest",
    Date: "Apr 26, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
  {
    Id: "forest-trail",
    Src: "./assets/trips/01/DSC00551.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00551.JPEG",
    Title: "Forest Trail",
    Alt: "Trail benches beneath tall trees in Olympic National Park",
    Date: "Apr 26, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
  {
    Id: "river-valley",
    Src: "./assets/trips/01/DSC00602.JPEG",
    CachedSrc: "./assets/trips/01/Cached/Cached_DSC00602.JPEG",
    Title: "River Valley",
    Alt: "Person standing beside a river in Olympic National Park",
    Date: "Apr 26, 2026",
    Year: "2026",
    Trip: "01",
    Location: "Olympic National Park, USA",
  },
];

class MyPhotoFilter extends HTMLElement {
  constructor() {
    super();
    this.onFilterChange = this.onFilterChange.bind(this);
  }

  connectedCallback() {
    this.render();
    this.filterSelects = this.querySelectorAll("select");
    this.filterSelects.forEach((select) =>
      select.addEventListener("change", this.onFilterChange)
    );
  }

  disconnectedCallback() {
    this.filterSelects?.forEach((select) =>
      select.removeEventListener("change", this.onFilterChange)
    );
  }

  getSelectedValues(id) {
    const select = this.querySelector(`#${id}`);
    return Array.from(select.selectedOptions).map((option) => option.value);
  }

  onFilterChange() {
    const selectedTrips = this.getSelectedValues("photoSelectedTrips");
    const selectedYears = this.getSelectedValues("photoSelectedYears");
    const selectedLocations = this.getSelectedValues(
      "photoSelectedLocations"
    );

    const filteredPhotos = window.photos.filter((photo) => {
      const matchesTrip =
        !selectedTrips.length || selectedTrips.includes(photo.Trip);
      const matchesYear =
        !selectedYears.length || selectedYears.includes(photo.Year);
      const matchesLocation =
        !selectedLocations.length ||
        selectedLocations.includes(photo.Location);

      return matchesTrip && matchesYear && matchesLocation;
    });

    document.querySelector("my-photo-gallery").setPhotos(filteredPhotos);
  }

  renderOptions(values, getLabel = (value) => value) {
    return values
      .map((value) => `<option value="${value}">${getLabel(value)}</option>`)
      .join("");
  }

  render() {
    const trips = [...new Set(window.photos.map((photo) => photo.Trip))].sort();
    const years = [...new Set(window.photos.map((photo) => photo.Year))].sort(
      (a, b) => b.localeCompare(a)
    );
    const locations = [
      ...new Set(window.photos.map((photo) => photo.Location)),
    ].sort();

    this.innerHTML = `
      <div class="filters-div photo-filters" aria-label="Photo filters">
        <select id="photoSelectedTrips" aria-label="Filter photos by trip" placeholder="Select Trip" txtSearch="Search Trip" multiple multiselect-search="true">
          ${this.renderOptions(trips, (trip) => window.photoTrips[trip].Name)}
        </select>
        <select id="photoSelectedYears" aria-label="Filter photos by year" placeholder="Select Year" txtSearch="Search Year" multiple multiselect-search="true">
          ${this.renderOptions(years)}
        </select>
        <select id="photoSelectedLocations" aria-label="Filter photos by location" placeholder="Select Location" txtSearch="Search Location" multiple multiselect-search="true">
          ${this.renderOptions(locations)}
        </select>
      </div>`;
  }
}

class MyPhotoGallery extends HTMLElement {
  constructor() {
    super();
    this.photos = [...window.photos];
    this.activePhotoIndex = 0;
    this.previousFocus = null;
    this.previousBodyOverflow = "";
    this.toastTimer = null;
    this.lightboxControlsTimer = null;
    this.lightboxClickGuardTimer = null;
    this.lightboxLastInput = "pointer";
    this.lightboxSwipe = null;
    this.suppressNextLightboxClick = false;
    this.photoLoadRequestId = 0;
    this.pendingFullImages = new Map();
    this.loadedFullImageSources = new Set();
    this.onGalleryClick = this.onGalleryClick.bind(this);
    this.onGalleryKeyDown = this.onGalleryKeyDown.bind(this);
    this.onLightboxPointerActivity =
      this.onLightboxPointerActivity.bind(this);
    this.onLightboxPointerDown = this.onLightboxPointerDown.bind(this);
    this.onLightboxPointerUp = this.onLightboxPointerUp.bind(this);
    this.onLightboxPointerCancel = this.onLightboxPointerCancel.bind(this);
    this.onLightboxFocusIn = this.onLightboxFocusIn.bind(this);
    this.onLightboxFocusOut = this.onLightboxFocusOut.bind(this);
  }

  connectedCallback() {
    this.render();
    this.addEventListener("click", this.onGalleryClick);
    this.addEventListener("keydown", this.onGalleryKeyDown);
    this.addEventListener("pointermove", this.onLightboxPointerActivity);
    this.addEventListener("pointerdown", this.onLightboxPointerDown);
    this.addEventListener("pointerup", this.onLightboxPointerUp);
    this.addEventListener("pointercancel", this.onLightboxPointerCancel);
    this.addEventListener("focusin", this.onLightboxFocusIn);
    this.addEventListener("focusout", this.onLightboxFocusOut);
  }

  disconnectedCallback() {
    this.removeEventListener("click", this.onGalleryClick);
    this.removeEventListener("keydown", this.onGalleryKeyDown);
    this.removeEventListener("pointermove", this.onLightboxPointerActivity);
    this.removeEventListener("pointerdown", this.onLightboxPointerDown);
    this.removeEventListener("pointerup", this.onLightboxPointerUp);
    this.removeEventListener("pointercancel", this.onLightboxPointerCancel);
    this.removeEventListener("focusin", this.onLightboxFocusIn);
    this.removeEventListener("focusout", this.onLightboxFocusOut);
    window.clearTimeout(this.toastTimer);
    this.clearLightboxControlsTimer();
    window.clearTimeout(this.lightboxClickGuardTimer);
    this.lightboxSwipe = null;
    this.cancelPendingPhotoLoads();
  }

  setPhotos(photos) {
    this.closeLightbox();
    this.photos = [...photos];
    this.render();
  }

  getPhotoFilename(photo) {
    return photo.Src.split("/").pop();
  }

  getPhotoUrl(photo) {
    return new URL(photo.Src, document.baseURI).href;
  }

  fallbackCopyText(text) {
    const textarea = document.createElement("textarea");
    const previousFocus = document.activeElement;
    textarea.value = text;
    textarea.setAttribute("readonly", "");
    textarea.style.position = "fixed";
    textarea.style.opacity = "0";
    textarea.style.pointerEvents = "none";
    document.body.appendChild(textarea);
    textarea.select();
    const copied = document.execCommand?.("copy");
    textarea.remove();
    previousFocus?.focus?.();

    if (!copied) throw new Error("Copy command was unavailable");
  }

  async sharePhotoLink(photo, trigger) {
    const photoUrl = this.getPhotoUrl(photo);
    const shareData = {
      title: photo.Title,
      url: photoUrl,
    };
    let canShareNatively = typeof navigator.share === "function";

    if (canShareNatively && typeof navigator.canShare === "function") {
      try {
        canShareNatively = navigator.canShare(shareData);
      } catch {
        canShareNatively = false;
      }
    }

    if (canShareNatively) {
      try {
        await navigator.share(shareData);
        this.showToast("Photo shared.", trigger, "success");
        return;
      } catch (error) {
        if (error?.name === "AbortError") return;
      }
    }

    try {
      let copied = false;
      if (navigator.clipboard?.writeText) {
        try {
          await navigator.clipboard.writeText(photoUrl);
          copied = true;
        } catch {
          copied = false;
        }
      }

      if (!copied) this.fallbackCopyText(photoUrl);
      this.showToast("Photo share link copied.", trigger, "success");
    } catch {
      this.showToast("Unable to share photo link", trigger, "error");
    }
  }

  showToast(message, trigger, state) {
    window.clearTimeout(this.toastTimer);
    this.querySelectorAll(".photo-toast").forEach((toast) =>
      toast.classList.remove("is-visible")
    );

    const lightbox = trigger.closest(".photo-lightbox");
    const toast = lightbox
      ? lightbox.querySelector('[data-photo-toast="lightbox"]')
      : this.querySelector('[data-photo-toast="gallery"]');
    if (!toast) return;

    toast.textContent = message;
    toast.dataset.state = state;
    toast.classList.add("is-visible");
    this.toastTimer = window.setTimeout(
      () => toast.classList.remove("is-visible"),
      2400
    );
  }

  getDownloadIcon() {
    return `
      <svg class="photo-action-icon photo-download-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M12 3v12m0 0 5-5m-5 5-5-5M5 21h14"></path>
      </svg>`;
  }

  getShareIcon() {
    return `
      <svg class="photo-action-icon photo-share-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <circle cx="18" cy="5" r="3"></circle>
        <circle cx="6" cy="12" r="3"></circle>
        <circle cx="18" cy="19" r="3"></circle>
        <path d="m8.6 10.5 6.8-4M8.6 13.5l6.8 4"></path>
      </svg>`;
  }

  renderCard(photo) {
    const photoIndex = this.photos.indexOf(photo);
    return `
      <article class="photo-card">
        <button class="photo-card-button" type="button" data-photo-index="${photoIndex}" aria-label="View ${photo.Title} full size">
          <img class="photo-card-image" src="${photo.CachedSrc}" alt="${photo.Alt}" loading="lazy" decoding="async">
        </button>
        <div class="photo-card-copy">
          <div class="photo-card-details">
            <div class="photo-card-title-container">
              <section class="photo-card-title">${photo.Title}</section>
              <div class="photo-card-actions">
                <button class="photo-control photo-share-button photo-card-action" type="button" data-share-photo-index="${photoIndex}" data-tooltip="Share" aria-label="Share ${photo.Title}">${this.getShareIcon()}</button>
                <a class="photo-control photo-download photo-card-action photo-card-download" href="${photo.Src}" download="${this.getPhotoFilename(photo)}" data-tooltip="Download" aria-label="Download ${photo.Title}">${this.getDownloadIcon()}</a>
              </div>
            </div>
            <p class="photo-card-meta">${photo.Location}&nbsp;•&nbsp;${photo.Date}</p>
          </div>
        </div>
      </article>`;
  }

  renderTripGroups() {  
    const tripGroups = this.photos.reduce((groups, photo) => {
      if (!groups.has(photo.Trip)) groups.set(photo.Trip, []);
      groups.get(photo.Trip).push(photo);
      return groups;
    }, new Map());

    return Array.from(tripGroups.entries())
      .map(([tripId, photos]) => {
        const tripName = window.photoTrips[tripId]?.Name ?? tripId;
        const tripHeaderId = `trip-${tripId}`;

        return `
          <section class="photo-trip-group" aria-labelledby="${tripHeaderId}">
            <section id="${tripHeaderId}" class="year-title linked-title">
              <my-copy-link target-id="${tripHeaderId}" target-type="trip"></my-copy-link>
              <span>${tripName}</span>
            </section>
            <div class="photo-grid">
              ${photos.map((photo) => this.renderCard(photo)).join("")}
            </div>
          </section>`;
      })
      .join("");
  }

  renderLightbox() {
    if (!this.photos.length) return "";

    const photo = this.photos[0];
    return `
      <dialog class="photo-lightbox" aria-labelledby="photoLightboxTitle" tabindex="-1">
        <div class="photo-lightbox-shell">
          <div class="photo-lightbox-topbar">
            <div class="photo-lightbox-caption">
              <div class="photo-lightbox-title-row">
                <strong id="photoLightboxTitle">${photo.Title}</strong>
                <span class="photo-lightbox-loader" role="status" aria-label="Loading full-resolution photo" hidden></span>
              </div>
              <span class="photo-lightbox-meta">${photo.Location}&nbsp; • &nbsp;${photo.Date}</span>
            </div>
            <div class="photo-lightbox-actions" role="group" aria-label="Photo actions">
              <button class="photo-control photo-share-button photo-lightbox-share" type="button" data-share-photo-index="0" aria-label="Share ${photo.Title}">${this.getShareIcon()}<span>Share</span></button>
              <a class="photo-control photo-download photo-lightbox-download" href="${photo.Src}" download="${this.getPhotoFilename(photo)}" aria-label="Download ${photo.Title}">${this.getDownloadIcon()}<span>Download</span></a>
            </div>
            <button class="photo-lightbox-close" type="button" data-lightbox-action="close" aria-label="Close full-size photo">&times;</button>
          </div>
          <div class="photo-toast" data-photo-toast="lightbox" role="status" aria-live="polite" aria-atomic="true"></div>
          <button class="photo-lightbox-nav photo-lightbox-previous" type="button" data-lightbox-action="previous" aria-label="Show previous photo">&#10094;</button>
          <figure class="photo-lightbox-figure">
            <img class="photo-lightbox-image" src="${photo.CachedSrc}" alt="${photo.Alt}" decoding="async">
          </figure>
          <button class="photo-lightbox-nav photo-lightbox-next" type="button" data-lightbox-action="next" aria-label="Show next photo">&#10095;</button>
        </div>
      </dialog>`;
  }

  onGalleryClick(event) {
    const lightbox = event.target.closest(".photo-lightbox");
    if (lightbox && this.suppressNextLightboxClick) {
      this.suppressNextLightboxClick = false;
      window.clearTimeout(this.lightboxClickGuardTimer);
      event.preventDefault();
      return;
    }

    const shareButton = event.target.closest("[data-share-photo-index]");
    if (shareButton) {
      event.preventDefault();
      const photo = this.photos[Number(shareButton.dataset.sharePhotoIndex)];
      if (photo) void this.sharePhotoLink(photo, shareButton);
      return;
    }

    const cardButton = event.target.closest("[data-photo-index]");
    if (cardButton) {
      this.openLightbox(Number(cardButton.dataset.photoIndex), cardButton);
      return;
    }

    const lightboxAction = event.target.closest("[data-lightbox-action]");
    if (lightboxAction) {
      const action = lightboxAction.dataset.lightboxAction;
      if (action === "close") this.closeLightbox();
      if (action === "previous") this.showPhoto(this.activePhotoIndex - 1);
      if (action === "next") this.showPhoto(this.activePhotoIndex + 1);
      return;
    }

    if (event.target.matches(".photo-lightbox")) this.closeLightbox();
  }

  onGalleryKeyDown(event) {
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog?.open) return;

    this.lightboxLastInput = "keyboard";
    this.showLightboxControls();

    if (event.key === "Escape") {
      event.preventDefault();
      this.closeLightbox();
    } else if (event.key === "ArrowLeft") {
      event.preventDefault();
      this.showPhoto(this.activePhotoIndex - 1);
    } else if (event.key === "ArrowRight") {
      event.preventDefault();
      this.showPhoto(this.activePhotoIndex + 1);
    }
  }

  openLightbox(index, trigger) {
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog || !this.photos.length) return;

    this.previousFocus = trigger;
    this.previousBodyOverflow = document.body.style.overflow;
    this.showPhoto(index);
    dialog.showModal();
    document.body.style.overflow = "hidden";
    this.lightboxLastInput = "pointer";
    this.showLightboxControls();
    dialog.focus({ preventScroll: true });
  }

  closeLightbox() {
    const dialog = this.querySelector(".photo-lightbox");
    this.clearLightboxControlsTimer();
    window.clearTimeout(this.lightboxClickGuardTimer);
    this.lightboxSwipe = null;
    this.suppressNextLightboxClick = false;
    this.cancelPendingPhotoLoads();
    if (!dialog?.open) return;

    dialog.classList.remove("is-controls-hidden", "is-loading-photo");
    dialog.close();
    document.body.style.overflow = this.previousBodyOverflow;
    if (this.previousFocus?.isConnected) this.previousFocus.focus();
  }

  clearLightboxControlsTimer() {
    window.clearTimeout(this.lightboxControlsTimer);
    this.lightboxControlsTimer = null;
  }

  isLightboxControl(element) {
    return Boolean(
      element?.closest?.(".photo-lightbox-topbar, .photo-lightbox-nav")
    );
  }

  hideLightboxControls() {
    this.lightboxControlsTimer = null;
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog?.open || dialog.classList.contains("is-loading-photo")) return;

    if (
      this.lightboxLastInput === "keyboard" &&
      this.isLightboxControl(document.activeElement)
    ) {
      return;
    }

    dialog.classList.add("is-controls-hidden");
  }

  scheduleLightboxControlsHide() {
    this.clearLightboxControlsTimer();
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog?.open || dialog.classList.contains("is-loading-photo")) return;

    this.lightboxControlsTimer = window.setTimeout(
      () => this.hideLightboxControls(),
      2500
    );
  }

  showLightboxControls() {
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog?.open) return;

    dialog.classList.remove("is-controls-hidden");
    this.scheduleLightboxControlsHide();
  }

  setLightboxImageLoading(isLoading) {
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog) return;

    dialog.classList.toggle("is-loading-photo", isLoading);
    if (isLoading) {
      this.clearLightboxControlsTimer();
      dialog.classList.remove("is-controls-hidden");
    } else {
      this.scheduleLightboxControlsHide();
    }
  }

  onLightboxPointerActivity(event) {
    const dialog = event.target.closest?.(".photo-lightbox");
    if (!dialog?.open) return;

    if (
      this.lightboxSwipe &&
      event.pointerId === this.lightboxSwipe.pointerId
    ) {
      const horizontalDistance = Math.abs(
        event.clientX - this.lightboxSwipe.startX
      );
      const verticalDistance = Math.abs(
        event.clientY - this.lightboxSwipe.startY
      );
      if (horizontalDistance > 8 && horizontalDistance > verticalDistance) {
        event.preventDefault();
      }
    }

    this.lightboxLastInput = "pointer";
    this.showLightboxControls();
  }

  onLightboxPointerDown(event) {
    const dialog = event.target.closest?.(".photo-lightbox");
    if (!dialog?.open) return;

    const controlsWereHidden = dialog.classList.contains("is-controls-hidden");
    this.lightboxLastInput = "pointer";
    this.showLightboxControls();

    const figure = event.target.closest?.(".photo-lightbox-figure");
    if (
      event.pointerType === "touch" &&
      event.isPrimary &&
      figure &&
      !this.lightboxSwipe
    ) {
      this.lightboxSwipe = {
        pointerId: event.pointerId,
        startX: event.clientX,
        startY: event.clientY,
        availableWidth: figure.clientWidth,
      };
    }

    if (controlsWereHidden && event.target.closest?.(".photo-lightbox-shell")) {
      this.suppressNextLightboxClick = true;
      window.clearTimeout(this.lightboxClickGuardTimer);
      this.lightboxClickGuardTimer = window.setTimeout(() => {
        this.suppressNextLightboxClick = false;
      }, 600);
    }
  }

  onLightboxPointerUp(event) {
    const swipe = this.lightboxSwipe;
    if (!swipe || event.pointerId !== swipe.pointerId) return;

    this.lightboxSwipe = null;
    const horizontalDistance = event.clientX - swipe.startX;
    const verticalDistance = event.clientY - swipe.startY;
    const swipeThreshold = Math.min(
      120,
      Math.max(50, swipe.availableWidth * 0.1)
    );

    if (
      Math.abs(horizontalDistance) < swipeThreshold ||
      Math.abs(horizontalDistance) <= Math.abs(verticalDistance) * 1.25
    ) {
      return;
    }

    event.preventDefault();
    this.showLightboxControls();
    this.showPhoto(
      this.activePhotoIndex + (horizontalDistance < 0 ? 1 : -1)
    );
    this.suppressNextLightboxClick = true;
    window.clearTimeout(this.lightboxClickGuardTimer);
    this.lightboxClickGuardTimer = window.setTimeout(() => {
      this.suppressNextLightboxClick = false;
    }, 600);
  }

  onLightboxPointerCancel(event) {
    if (event.pointerId === this.lightboxSwipe?.pointerId) {
      this.lightboxSwipe = null;
    }
  }

  onLightboxFocusIn(event) {
    const dialog = event.target.closest?.(".photo-lightbox");
    if (!dialog?.open) return;

    this.showLightboxControls();
    if (
      this.lightboxLastInput === "keyboard" &&
      this.isLightboxControl(event.target)
    ) {
      this.clearLightboxControlsTimer();
    }
  }

  onLightboxFocusOut() {
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog?.open) return;

    queueMicrotask(() => {
      if (!dialog.open) return;
      if (
        this.lightboxLastInput === "keyboard" &&
        this.isLightboxControl(document.activeElement)
      ) {
        this.clearLightboxControlsTimer();
        return;
      }

      this.scheduleLightboxControlsHide();
    });
  }

  invalidatePhotoDisplay() {
    this.photoLoadRequestId += 1;
  }

  cancelPendingFullImageLoad(fullImageUrl, loadRecord) {
    if (this.pendingFullImages.get(fullImageUrl) !== loadRecord) return;

    this.pendingFullImages.delete(fullImageUrl);
    loadRecord.image.onload = null;
    loadRecord.image.onerror = null;
    loadRecord.image.removeAttribute("src");
    loadRecord.reject(new Error(`Cancelled loading ${fullImageUrl}`));
  }

  cancelPendingPhotoLoads() {
    this.invalidatePhotoDisplay();

    for (const [fullImageUrl, loadRecord] of this.pendingFullImages) {
      this.cancelPendingFullImageLoad(fullImageUrl, loadRecord);
    }
  }

  getAdjacentPhotoIndexes(index) {
    if (this.photos.length < 2) return [];

    return [
      ...new Set([
        (index + 1) % this.photos.length,
        (index - 1 + this.photos.length) % this.photos.length,
      ]),
    ].filter((adjacentIndex) => adjacentIndex !== index);
  }

  cancelUnneededFullImageLoads(index) {
    const relevantIndexes = [index, ...this.getAdjacentPhotoIndexes(index)];
    const relevantSources = new Set(
      relevantIndexes.map((photoIndex) =>
        this.getPhotoUrl(this.photos[photoIndex])
      )
    );

    for (const [fullImageUrl, loadRecord] of this.pendingFullImages) {
      if (!relevantSources.has(fullImageUrl)) {
        this.cancelPendingFullImageLoad(fullImageUrl, loadRecord);
      }
    }
  }

  ensureFullImageLoaded(photo) {
    const fullImageUrl = this.getPhotoUrl(photo);
    if (this.loadedFullImageSources.has(fullImageUrl)) {
      return Promise.resolve(fullImageUrl);
    }

    const pendingLoad = this.pendingFullImages.get(fullImageUrl);
    if (pendingLoad) return pendingLoad.promise;

    const fullImage = new Image();
    let resolveLoad;
    let rejectLoad;
    const promise = new Promise((resolve, reject) => {
      resolveLoad = resolve;
      rejectLoad = reject;
    });
    const loadRecord = {
      image: fullImage,
      promise,
      reject: rejectLoad,
    };
    this.pendingFullImages.set(fullImageUrl, loadRecord);

    const rejectCurrentLoad = (error) => {
      if (this.pendingFullImages.get(fullImageUrl) !== loadRecord) return;

      this.pendingFullImages.delete(fullImageUrl);
      fullImage.onload = null;
      fullImage.onerror = null;
      rejectLoad(error);
    };

    fullImage.decoding = "async";
    fullImage.onload = async () => {
      if (typeof fullImage.decode === "function") {
        try {
          await fullImage.decode();
        } catch (error) {
          rejectCurrentLoad(error);
          return;
        }
      }

      if (this.pendingFullImages.get(fullImageUrl) !== loadRecord) return;

      this.pendingFullImages.delete(fullImageUrl);
      this.loadedFullImageSources.add(fullImageUrl);
      fullImage.onload = null;
      fullImage.onerror = null;
      resolveLoad(fullImageUrl);
    };
    fullImage.onerror = () =>
      rejectCurrentLoad(new Error(`Unable to load ${fullImageUrl}`));
    fullImage.src = photo.Src;

    return promise;
  }

  preloadAdjacentPhotos(index) {
    for (const adjacentIndex of this.getAdjacentPhotoIndexes(index)) {
      void this.ensureFullImageLoaded(this.photos[adjacentIndex]).catch(
        () => {}
      );
    }
  }

  showPhoto(index) {
    if (!this.photos.length) return;

    this.invalidatePhotoDisplay();
    const loadRequestId = this.photoLoadRequestId;
    this.activePhotoIndex =
      (index + this.photos.length) % this.photos.length;
    const photo = this.photos[this.activePhotoIndex];
    const dialog = this.querySelector(".photo-lightbox");
    if (!dialog) return;

    this.cancelUnneededFullImageLoads(this.activePhotoIndex);

    const image = dialog.querySelector(".photo-lightbox-image");
    const loader = dialog.querySelector(".photo-lightbox-loader");
    const selectedFullImageUrl = this.getPhotoUrl(photo);
    const isFullImageLoaded =
      this.loadedFullImageSources.has(selectedFullImageUrl);
    loader.hidden = isFullImageLoaded;
    this.setLightboxImageLoading(!isFullImageLoaded);
    image.alt = photo.Alt;
    image.src = photo.CachedSrc;

    dialog.querySelector("#photoLightboxTitle").textContent = photo.Title;
    dialog.querySelector(".photo-lightbox-meta").textContent =
      `${photo.Location} • ${photo.Date}`;
    const download = dialog.querySelector(".photo-lightbox-download");
    download.href = photo.Src;
    download.download = this.getPhotoFilename(photo);
    download.setAttribute("aria-label", `Download ${photo.Title}`);
    const share = dialog.querySelector(".photo-lightbox-share");
    share.dataset.sharePhotoIndex = String(this.activePhotoIndex);
    share.setAttribute("aria-label", `Share ${photo.Title}`);

    void this.ensureFullImageLoaded(photo)
      .then((loadedFullImageUrl) => {
        if (loadRequestId !== this.photoLoadRequestId) return;

        image.src = loadedFullImageUrl;
        loader.hidden = true;
        this.setLightboxImageLoading(false);
        this.preloadAdjacentPhotos(this.activePhotoIndex);
      })
      .catch(() => {
        if (loadRequestId === this.photoLoadRequestId) {
          loader.hidden = true;
          this.setLightboxImageLoading(false);
        }
      });
  }

  render() {
    window.clearTimeout(this.toastTimer);
    const tripCount = new Set(this.photos.map((photo) => photo.Trip)).size;
    const summary = `Showing ${this.photos.length} of ${window.photos.length} photos from ${tripCount} of ${Object.keys(window.photoTrips).length} trips.`;

    this.innerHTML = `
      <section class="filter-result filter-result-row photo-results">
        <span aria-live="polite">${summary}</span>
        <my-filter-toggle filter-target="photoFilters"></my-filter-toggle>
      </section>
      ${
        this.photos.length
          ? this.renderTripGroups()
          : `<div class="photo-empty" role="status">No photos match the selected filters.</div>`
      }
      ${this.renderLightbox()}
      <div class="photo-toast" data-photo-toast="gallery" role="status" aria-live="polite" aria-atomic="true"></div>`;

    const dialog = this.querySelector(".photo-lightbox");
    dialog?.addEventListener("cancel", (event) => {
      event.preventDefault();
      this.closeLightbox();
    });
  }
}

customElements.define("my-photo-filter", MyPhotoFilter);
customElements.define("my-photo-gallery", MyPhotoGallery);
